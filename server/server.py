#!/usr/bin/env python3
# Needed for Docker ENV things to run FLASK
# first app = package name (directory app/),
# second app = Flask class name (variable defined in /app/__init__.py)
from app import app
